# Configurações básicas do Spring Boot
spring.application.name=todolist

# Configurações do servidor
server.port=8080

# Configurações do datasource para PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/todolist_db
spring.datasource.username=juninhodb
spring.datasource.password=130593JU@
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações do JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuração de timezone (opcional)
spring.jackson.time-zone=UTC
